import { ReactNode } from 'react';

type Props = {
  children: ReactNode;
};

const Hero = (props: Props) => {
  const name = ['book', 'yam']
  name.filter
  return <div>Hero</div>;
};

export default Hero;
